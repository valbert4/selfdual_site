# T24 - Three-Block Mode-1 Per-Coset Tiling

Status: `saturates`

## Summary

This test composes the two filters that actually cut — the three-block split
(T05) and per-coset Mode-1 tiling (T18) — into one `16|16|40` per-coset tiling.
The composition is feasible for every surviving row.

## What It Tests

- Object: the `16|16|40` three-block split with per-coset tiling of the
  length-40 block.
- Input: the row `(k,a,b)`, the three-block split rows, and per-coset conical
  hulls.
- Necessary condition: a per-coset tiling of the three-block split exists.
- Output: rows with no valid three-block tiling (none, here).

## Result

- Kills: `0`.
- Status: saturates. The continuous freedom of the split family absorbs the
  `W = 16` cross-diagonal demand once the capped coset supply is ample.

## Verification

- Certificate type: exact-rational (QQ / PPL, polyhedron) feasibility.
- Reproduce: download the reproduction bundle below and run it.

## Reproduction bundle

[Download `T24-3bm-repro.tar.gz`](/downloads/repro/T24-3bm-repro.tar.gz) — a 🟡 solver-required bundle. Three-block Mode-1 per-coset tiling feasible for every row. Reproduce in the research repo (the bundle ships the source script and the exact command); for the proof-grade kills a one-click pure-Python verifier is the next upgrade. Checksums: [manifest.json](/downloads/repro/manifest.json).

## How To Help

Pin the residual two-parameter `(u,v)` freedom with the genus-3 triweight
residual data, or lift to pairwise coset coupling, where the slack may close.
