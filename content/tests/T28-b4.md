# T28 - Polak B4 Quadruple SDP

Status: `saturates; closed`

## Public Summary

This test runs the Polak four-point (B4) constant-weight SDP at length 40, the
strongest moment-hierarchy layer attempted on the menu. It provably cannot cut:
B4 equals the exact Delsarte bound at length 40.

## What It Tests

- Object: the weight-16 words of `E` under the four-point (B4) SDP hierarchy.
- Input: the row `(k,a,b)` and the B4 constraint system.
- Necessary condition: the required count is at most the B4 bound.
- Output: rows exceeding B4 (none possible).

## Result

- Kills: `0` (run on all surviving rows, 2026-06-12).
- Status: saturates and closed. The B4 optimum equals the exact Delsarte bound at
  `n = 40` (`{8} → 114.28`, `{4,8} → 247`, `{4,6,8} → 686.1`), all far above the
  rows' required counts. No length-40 kill is available from this hierarchy.

## Verification

- Certificate type: converged B4 SDP optima matching exact PPL Delsarte values.
- Reproduce: download the reproduction bundle below and run it.
  optima.

## Reproduction bundle

[Download `T28-b4-repro.tar.gz`](/downloads/repro/T28-b4-repro.tar.gz) — a 🟡 solver-required bundle. Polak B4 equals exact Delsarte at n=40 (no kill possible). Reproduce in the research repo (the bundle ships the source script and the exact command); for the proof-grade kills a one-click pure-Python verifier is the next upgrade. Checksums: [manifest.json](/downloads/repro/manifest.json).

## How To Help

This layer is closed — do not rebuild it unchanged. The next moment lever must be
anchored (length-72) rather than a higher hierarchy at length 40.
