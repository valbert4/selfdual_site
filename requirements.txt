# Python build dependencies for the Extremal 72 site.
# System dependency (not pip-installable): pandoc  (Markdown -> HTML).
#   Debian/Ubuntu: apt-get install pandoc   |   macOS: brew install pandoc
# The reproduction verifiers in repro/ use only the Python standard library.
PyYAML>=6.0
